UI components live here. shadcn/ui primitives are added with
`npx shadcn@latest add <component>` and land in `components/ui/`.
Feature components (review table, board, report view) get their own folders in phase 1+.
